%h3.page_title
  Deploy key:
  = @key.title
  %small
    created at
    = @key.created_at.stamp("Aug 21, 2011")
.back_link
  = link_to admin_keys_path do
    &larr; To keys list
%hr
%pre= @key.key
.pull-right
  = link_to 'Remove', [:admin, @key], confirm: 'Are you sure?', method: :delete, class: "btn btn-remove delete-key"
